import React from "react"
import { TextField, Button } from "mui/material"

export default function ChatBar(props){

	return (
		<Grid container>
			<Grid item>
				<TextField multiline onChange={} onKeyDown={} variant='filled'/>
			</Grid>
			<Grid item>

			</Grid>
		</Grid>
	);
}
